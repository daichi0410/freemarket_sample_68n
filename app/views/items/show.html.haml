商品名
= @item.name

値段
= @item.price

発送日の目安
=  @item.date
で発送

-# - if user_longin?
-#   = link_to "#", あああ
-# - elsif @item.user_id == current_user.id
-#   = link_to "#", 編集 
-#   = link_to "#", 削除
-# - else 
-#   =link_to "#", 購入